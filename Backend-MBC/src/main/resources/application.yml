server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mbc_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: SchoolProject@2023
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: "bXktdmVyeS1sb25nLXN1cGVyLXNlY3JldC1rZXktY2hhbmdlLW1lLWxhdGVyIQ==" # base64 string
    expiration-ms: 3600000        # 1 hour access token
    refresh-expiration-ms: 1209600000 # 14 days refresh

# CORS (Angular default dev origin)
cors:
  allowed-origins:
    - "http://localhost:4200"
